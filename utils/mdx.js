// /utils/mdx.js

// Placeholder: Add fs, path, gray-matter, and next-mdx-remote logic when ready

export function getAllPosts() {
  // Example: Read all slugs from /content/blog/
  return [];
}

export async function getPostBySlug(slug) {
  // Example: Read MDX file, parse frontMatter, serialize MDX source
  return { source: null, frontMatter: null };
}
