// /utils/getContent.js

// Placeholder: use real file reading (fs) and parsing when you add content files

export function getProjects() {
  // When ready, use fs and path to read from /content/projects/
  // e.g., return JSON.parse(fs.readFileSync(path.join(process.cwd(), 'content/projects/projects.json')));
  return [];
}

export function getSpeaking() {
  // When ready, use fs and path to read from /content/speaking/
  return [];
}

export function getTestimonials() {
  // When ready, use fs and path to read from /content/testimonials/
  return [];
}
